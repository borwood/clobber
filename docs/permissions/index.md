> **AUTOGENERATED — do not edit.** Regenerate with `bun run docs:gen`. Source: cli-capabilities.ts, commands/*, role manifests.

# Permissions

Capabilities grouped by access level. Internal capabilities are not listed.

## Read

| Capability | Description |
| ---------- | ----------- |
| `agents` | List live agents in the current workspace. |
| `notify.list` | List the calling agent's unacknowledged notifications. |
| `reports` | Read worker final-reports and findings. |
| `roles.diff` | Show working-copy changes against the checked-out role's branch tip. |
| `roles.list` | List roles available in the current workspace. |
| `roles.show` | Show a role's current version, history, and configuration. |
| `roles.status` | Show whether a role checkout is open and which files changed. |
| `roles.upstream.diff` | Line-level diff of the local role pin versus its upstream default. |
| `roles.upstream.log` | Branch changelog (no range) or upstream-ahead list (with @{upstream}..). |
| `self-skills.list` | List the current role's granted self-skills and workspace policy. |
| `transcript` | Read a session's transcript. |
| `whoami` | Return the caller's session and role identity. |

## Write

| Capability | Description |
| ---------- | ----------- |
| `ask` | Post a blocking question to the user and await the answer. |
| `finding` | Submit a freetext triage finding. |
| `message` | Send a message to another session in the workspace. |
| `notify.ack` | Acknowledge one of the calling agent's own notifications by ID. |
| `reply` | Reply to a received message. |
| `report` | Submit the session's structured final-report. |
| `status` | Post the agent's current status string. |
| `worktrees.set` | Move the caller's own agent worktree to a new path. |

## Admin

| Capability | Description |
| ---------- | ----------- |
| `cycle` | Kill and reseat the current session (tool-token-gated). |
| `kill` | Terminate a session in the current workspace. |
| `prompt-modules.edit` | Replace a prompt-module's definition in the workspace catalog. |
| `resume` | Resume or restart a session in the current workspace. |
| `roles.ceiling` | Set the spawn ceiling for a role in this workspace. |
| `roles.checkout` | Open a role's branch as a working copy in the desk. |
| `roles.commit` | Commit working-copy edits onto the role branch and advance the pin. |
| `roles.delete` | Remove a role, its fork-branch, and its config refs. |
| `roles.discard` | Discard working-copy changes without committing. |
| `roles.edit` | Apply a one-shot patch to a role without a working-copy round-trip. |
| `roles.fetch` | Refresh upstream remote-tracking refs in the workspace role clone. |
| `roles.fork` | Create a new role as a fresh branch forked from a source role. |
| `roles.prompt-modules.add` | Append a prompt-module ref to a role. |
| `roles.prompt-modules.toggle` | Toggle a prompt-module ref's enabled flag on a role. |
| `roles.wake-programs.add` | Add a wake-program to a role. |
| `roles.wake-programs.edit` | Edit an existing wake-program on a role. |
| `roles.wake-programs.remove` | Remove a wake-program from a role. |
| `self-skills.grant` | Grant a skill to the current role (within workspace policy). |
| `self-skills.release` | Remove a granted self-skill from the current role. |
| `spawn` | Spawn a new agent in the current workspace. |
| `worktrees.set-agent` | Move a named agent's worktree to a new path (agent must be idle). |
| `worktrees.set-default` | Set the workspace default branch-prefix convention for new agent worktrees. |
